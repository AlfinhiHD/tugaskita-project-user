export type LoginValue = {
    email: string,
    password: string,
    rememberMe: boolean
}

export type TugasDetail = {
    judul: string;
    poin: number;
    tenggat: string;
    deskripsi: string;
  }